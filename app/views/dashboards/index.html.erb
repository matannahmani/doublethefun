
<ul class="nav nav-tabs" id="myTab" role="tablist">
  <li class="nav-item">
    <a class="nav-link active" id="home-tab" data-toggle="tab" href="#hosted" role="tab" aria-controls="home" aria-selected="true">Events Hosted</a>
  </li>
  <li class="nav-item">
    <a class="nav-link" id="profile-tab" data-toggle="tab" href="#joined" role="tab" aria-controls="profile" aria-selected="false">Events Joined</a>
  </li>
</ul>
<div class="tab-content" id="myTabContent">
  <div class="tab-pane fade show active" id="hosted" role="tabpanel" aria-labelledby="home-tab">
 <!--    <% @my_events.each do |event| %>
    <%= event.name %>
      <% if policy(event).show? %>
        <%= link_to "Show", event_path(event) %>
      <% end %>
    <% end %>
 -->
  <div class="container">
    <div class="cardbox">
      <% if !@events.nil? %>
        <% @my_events.each do |event|  %>
        <a href="<%= event_path(event) %>">
          <div class="card-trip">
            <i class="fas fa-user-plus"></i>
            <% if event.photo.attached? %>
              <%= cl_image_tag event.photo.key, height: 150, width: 150, crop: :fill %>
            <% else %>
              <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg" />
            <%end%>
            <div class="card-trip-infos">
              <div>
                <h2><%= event.name %></h2>
                <p><%= event.description[0..10] %></p>
              </div>
              <h2 class="card-trip-pricing"><%= event.location %></h2>
              <% if User.find(event.user_id).photo.attached? %>
                <%= cl_image_tag User.find(event.user_id).photo.key, class: :"card-trip-user avatar-bordered" %>
              <% else %>
                <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" />
              <% end %>
            </div>
          </div>
        </a>
        <% end %>
      <% end %>
    </div>
  </div>

</div>
  <div class="tab-pane fade" id="joined" role="tabpanel" aria-labelledby="profile-tab">
    <% if !@my_attendances.nil? %>
      <% @my_attendances.each do |attendance| %>
        <%= attendance %>
        <% if policy(attendance).show? %>
          <%= link_to "Show", attendance_path(attendance) %>
        <% end %>
      <% end %>
    <% else %>
      <h3>You haven't joined any events yet</h3>
    <% end %>

  <div class="container">
    <div class="cardbox">
      <% if !@events.nil? %>
        <% @my_attendances.each do |event|  %>
        <a href="<%= event_path(event) %>">
          <div class="card-trip">
            <i class="fas fa-user-plus"></i>
            <% if event.photo.attached? %>
              <%= cl_image_tag event.photo.key, height: 150, width: 150, crop: :fill %>
            <% else %>
              <img src="https://raw.githubusercontent.com/lewagon/fullstack-images/master/uikit/greece.jpg" />
            <%end%>
            <div class="card-trip-infos">
              <div>
                <h2><%= event.name %></h2>
                <p><%= event.description[0..10] %></p>
              </div>
              <h2 class="card-trip-pricing"><%= event.location %></h2>
              <% if User.find(event.user_id).photo.attached? %>
                <%= cl_image_tag User.find(event.user_id).photo.key, class: :"card-trip-user avatar-bordered" %>
              <% else %>
                <img src="https://kitt.lewagon.com/placeholder/users/krokrob" class="card-trip-user avatar-bordered" />
              <% end %>
            </div>
          </div>
        </a>
        <% end %>
      <% end %>
    </div>
  </div>
  </div>
</div>
